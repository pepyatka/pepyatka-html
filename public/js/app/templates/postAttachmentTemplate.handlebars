<div class="attachment">
  <a href="{{attachment.model.url}}" title="{{attachment.model.id}}">
    {{#if attachment.model.thumbnailUrl}}
      <img src="{{attachment.model.thumbnailUrl}}" alt="{{attachment.model.id}}" />
    {{else}}
      {{attachment.model.id}}
    {{/if}}
  </a>
</div>
