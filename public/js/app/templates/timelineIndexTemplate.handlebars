{{view "profile-links"}}

{{#if controller.myFeed}}
  {{#if controller.firstPage}}
    {{view "submit-post"}}
  {{/if}}
{{/if}}

{{#unless controller.firstPage}}
  {{view "pagination"}}
{{/unless}}
{{view "posts"}}
{{view "pagination"}}
