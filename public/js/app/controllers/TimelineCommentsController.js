define(["app/app",
        "controllers/TimelineGenericController"], function(App) {
  "use strict";

  // Timeline on /username/comments
  App.TimelineCommentsController = App.TimelineGenericController.extend()
})
